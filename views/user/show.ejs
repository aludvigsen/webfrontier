<div class='container'>
	<h1><%= user.name %> </h1>
	<% if (user.admin) { %>
		<img src="/images/user/admin.png">admin
	<% } else { %>
		<img src="/images/user/pawn.png">pawn
	<% } %>
	<hr>
	<h3>Epost: <%= user.email %> </h3>
	
	<a class="btn btn-medium btn-primary" href="/user/edit/<%= user.id %>">Edit</a>
	
</div>